{
  "name": "ensombl-auth",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=24 <25",
    "pnpm": ">=11 <12"
  },
  "packageManager": "pnpm@11.17.0",
  "scripts": {
    "dev": "pnpm docker:up",
    "build": "tsc --noEmit",
    "check": "pnpm lint && pnpm build && pnpm test && pnpm validate:compose",
    "test": "vitest run",
    "lint": "biome check .",
    "format": "biome check --write .",
    "bootstrap": "tsx src/cli.ts",
    "docker:up": "docker compose up -d --wait && docker compose build product-bootstrap && docker compose run --rm product-bootstrap",
    "docker:down": "docker compose down",
    "docker:reset": "docker compose down --volumes",
    "docker:logs": "docker compose logs -f zitadel-api zitadel-login",
    "validate:compose": "docker compose config --quiet && BWS_ACCESS_TOKEN=compose-validation BWS_PROJECT_ID=00000000-0000-0000-0000-000000000000 docker compose -f deploy/dokploy/compose.yml config --quiet"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.15",
    "@types/node": "24.10.1",
    "tsx": "4.21.0",
    "typescript": "5.9.3",
    "vitest": "4.1.0",
    "zod": "4.3.6"
  }
}
